<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nhatdd.nhanambook">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="activity.HomeActivity" />
        <activity
            android:name="activity.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name="activity.UserActivity" />
        <activity android:name="activity.BookActivity" />
        <activity android:name="activity.BillActivity" />
        <activity android:name="activity.RevenueActivity" />
        <activity android:name="activity.SellActivity" />
        <activity android:name="activity.InfoBillActivity" />
        <activity android:name="activity.EditInfoUserActivity" />
        <activity android:name="activity.EditBookTypeActivity" />
        <activity android:name="activity.AddBillInfoActivity" />
        <activity android:name="activity.AddBillActivity" />
        <activity android:name="activity.AddUserActivity" />
        <activity android:name="activity.AddBookActivity" />
        <activity android:name="activity.ChangePasswActivity" />
        <activity android:name="activity.BooktypeActivity" />
        <activity android:name="activity.AddBooktypeActivity"/>
    </application>

</manifest>